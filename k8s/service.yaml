apiVersion: v1
kind: Service
metadata:
  name: orders-service
  namespace: sre-lab
  labels:
    app: orders-service
spec:
  type: NodePort
  selector:
    app: orders-service
  ports:
    - name: http
      port: 8080
      targetPort: http
      nodePort: 30080
